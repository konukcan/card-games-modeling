## Score
8.5/10

## Ready
Almost

## Reviewer commit inspected
9d97392 (via direct python -c probes)

## Rulings on Round 2 findings

### R2-Fix1 (sample_program root-type agreement): OVERRULED
Unify added at mcmc_search.py:148; test_sample_program_retry_path_enforces_root_type_agreement locks in the invariant (seeds 900..1199 at max_depth ∈ {5,6} with 0-leak).

### R2-Fix2 (W2 counters attached to MCMCResult): OVERRULED at harness level, PARTIALLY SUSTAINED at experiment-artifact level
MCMCResult/run_parallel_chains wiring is clean. But no review-stage/experiments/* script asserts zero on its reported result. review-stage/experiments/round_1/posterior_calibration_v2.py can still silently take an approximation branch without any signal.

### R2-Fix3 (weakened typability test): PARTIALLY SUSTAINED
Prose/assert still slightly overstate the invariant. Reviewer reproduced log_q_rev=-inf on seed=10+7919 where typability and non-empty new_sites both hold. Variable `reversible_given_typed` promises finite-density but only checks non-empty reverse sites.

### R2-Fix4 (tiny exact binary grammar + Monte-Carlo): OVERRULED
Structural key + N=20000 with 4σ tolerance close depth-3 exact-scorer agreement at the toy scale.

## NEW findings (Round 3)

### F1 — Medium: no-terminal lookahead branch uncovered
Location: _score_depth_cap_lookahead_exact at mcmc_search.py:~1192
The branch fires when (depth >= max_depth) AND (no terminal) AND (no var). Current tests do not exercise a grammar with zero terminal productions at the target type.
Recommended grammar: {p:INT→BOOL, q:INT→BOOL, zero:INT, one:INT} plus optional non-terminable r:LIST_INT→BOOL.
Closed-form support over BOOL at depth=0 max_depth=0: 4 programs uniform at 1/4.
MINIMUM fix: add a closed-form lookahead grammar test + Monte Carlo companion.

### F2 — Medium: experiment-side W2 assertion missing
Location: review-stage/experiments/round_1/posterior_calibration_v2.py
Does not reset or record approximation_fallback_counters. Counters could be nonzero in a future run and never surface in JSON/log.
MINIMUM fix: reset on chain start, record per-seed, assert aggregate zero, persist in JSON.

### F3 — Low/Medium: prose/assert mismatch in R2-Fix3
Location: test_mcmc_search.py::test_propose_regeneration_preserves_typability_and_reversibility
Either rename reversible_given_typed → has_reverse_sites_given_typed and disclaim log_q_rev may be -inf, OR strengthen assertion to math.isfinite(log_q_rev).

## Reviewer memory update for Round 4 (verbatim)
> Addressed: R2-Fix1 root-type agreement is clean; R2-Fix2 harness-level counter wiring is clean; R2-Fix4 binary grammar closure is clean.
> Still open: no-terminal lookahead branch uncovered (F1); experiment-side W2 assertion missing (F2); R2-Fix3 prose/assert mismatch (F3); init-resampling manuscript caveat.
> New suspicions: silent approximation fallbacks still reachable on experiment scripts that bypass the MCMCChain harness.
> Pattern: theoretical-correctness core is firm; remaining risk is operational — coverage gaps at branch boundaries + experiment artifacts that don't surface the new guardrails.

---
Thread ID: 019da4d5-c81e-77b2-9c4d-e64b2b463c93
